#include <stdio.h>

void main(void)
{
    printf("the type variables int use %d bytes\n", sizeof(int));
    printf("the type variables float use %d bytes\n", sizeof(float));
    printf("the type variables double use %d bytes\n", sizeof(double));
    printf("the type variables unsigned use %d bytes\n", sizeof(unsigned));
    printf("the type variables long use %d bytes\n", sizeof(long));
    
}